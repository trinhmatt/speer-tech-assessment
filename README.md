# To run application 

### Install dependencies
     yarn install
     
### Start server 
     node server.js

### Run tests (server must be started)
     yarn test

<br />
Server runs on localhost:8080  
  
